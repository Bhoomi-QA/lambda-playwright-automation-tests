{
  "name": "playwright-ts-hyperexecute-assignment",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npx playwright install --with-deps",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^24.3.0",
    "dotenv": "^17.2.3",
    "typescript": "^5.4.0"
  }
}
